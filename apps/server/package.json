{
  "name": "server",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsdown",
    "check-types": "tsc -b",
    "compile": "bun build --compile --minify --sourcemap --bytecode ./src/index.ts --outfile server",
    "deploy": "alchemy deploy",
    "destroy": "alchemy destroy",
    "test:e2e": "ALCHEMY_E2E=1 alchemy dev",
    "test": "bun run ./test/connect.test.ts"
  },
  "dependencies": {
    "hono": "^4.8.2",
    "dotenv": "catalog:",
    "zod": "catalog:",
    "@cloudbrowsers/db": "workspace:*"
  },
  "devDependencies": {
    "@cloudbrowsers/config": "workspace:*",
    "@cloudflare/containers": "^0.0.31",
    "@cloudflare/workers-types": "catalog:",
    "@types/node": "catalog:",
    "alchemy": "^0.81.2",
    "puppeteer-core": "^24.34.0",
    "tsdown": "^0.16.5",
    "typescript": "catalog:",
    "wrangler": "^4.54.0"
  }
}
